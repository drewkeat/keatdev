import React from "react"
import {Button} from "@mui/material"

const Header=(props)=>{
  return(
    <>
      <h1>Diff Header</h1>
      <Button variant="contained" primary>Click Me</Button>
    </>
  )
}

export default Header